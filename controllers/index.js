const userControllers = require('./userController');
const commentControllers = require('./commentController');
const blogControllers = require('./blogController');

module.exports = {
    userControllers,
    commentControllers,
    blogControllers,
};