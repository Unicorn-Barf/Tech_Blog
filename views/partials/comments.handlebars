    <div class="card-header">
        <h5 class="card-title">{{user.username}}</h5>
        <h6 class="card-subtitle mb-2 text-muted">Posted by {{user.username}} on {{toDate createdAt}}</h6>
    </div>
    <div class="card-body">
        <p class="card-text">{{comment}}</p>
    </div>