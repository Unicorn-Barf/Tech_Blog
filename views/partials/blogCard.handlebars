<div class="card mb-5">
    <div class="card-header">
       <h2>{{title}}</h2>
       <p>Posted by {{user.username}} on {{toDate createdAt}}</p>
    </div>
    <div class="card-body">
        <p class="card-text">{{blog}}</p>
    </div>
</div>